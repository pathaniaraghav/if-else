#include<stdio.h>
int main(){
    char ch;
    printf("Enter anything : ");
    scanf("%c",&ch);
    if (ch >= 'a' && ch <= 'z'){
        printf("Chracter is Alphabet");
    }
    else{
        printf("character is not alphabet");
    }
    return 0;
}
